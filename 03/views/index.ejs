<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>URL Shortner</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="url" type="text" class="ghost-input" placeholder="Enter URL" required>
          <input type="submit" class="ghost-button" value="Shorten URL">
        </form>
        <% if(locals.url !== null){ %>
          <p><%= locals.url %> </p>
        <% } %>

        <% if(locals.error !== null){ %>
          <p><%= locals.error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>